
export class Server {
  async start() {
    // Aquí inicia el servidor

    // Se settea la ruta de los usuarios, por ejemplo:
    // const userRoutes = new UserRoutes();
    // this.app.use('/users', userRoutes.getRouter());
  }

  async stop() {
    // Aquí detiene el servidor
  }
}
